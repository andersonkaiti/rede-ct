import { NavigationBar } from "@/app/(public)/_components/navigation-bar/navigation-bar";

// Simple re-export so that other modules can use the conventional `Header` component
// without changing the existing navigation-bar implementation that already exists
// in the codebase.
export default NavigationBar;
